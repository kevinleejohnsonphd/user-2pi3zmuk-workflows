"""
Test suite for Medical triage agent that analyzes patient symptoms, test results, and doctor observations to assign a triage tier (1: immediate, 2: within 1 year, 3: beyond 1 year) based on diagnosis severity and risk level.

Auto-generated by AI Agent Builder.
"""

import pytest
from langchain_core.messages import HumanMessage, AIMessage
from graph import create_graph


@pytest.mark.asyncio
async def test_happy_path_severe_chest_pain_requiring_immediate_attention():
    """
    Test: Happy path - severe chest pain requiring immediate attention
    
    Success criteria: Correctly assigns Tier 1 with appropriate justification for acute MI
    """
    graph = create_graph()
    
    initial_state = {
        "messages": [
            HumanMessage(content="Patient is a 58-year-old male presenting with severe crushing chest pain radiating to left arm for the past 30 minutes. Sweating profusely, shortness of breath. BP: 160/95, HR: 110. ECG shows ST-segment elevation. Troponin levels elevated at 0.8 ng/mL. Doctor notes possible acute MI.")
        ]
    }
    
    result = await graph.ainvoke(initial_state)
    
    # Verify response exists
    assert "messages" in result
    assert len(result["messages"]) > 1
    
    # Get final AI response
    final_message = result["messages"][-1]
    assert isinstance(final_message, AIMessage)
    
    response_text = final_message.content.lower()
    
    # Verify Tier 1 assignment
    assert "tier 1" in response_text or "triage tier 1" in response_text
    
    # Verify key clinical reasoning elements
    assert any(term in response_text for term in ["stemi", "st-segment elevation", "myocardial infarction", "acute mi"])
    assert "immediate" in response_text or "emergency" in response_text


@pytest.mark.asyncio
async def test_happy_path_mild_condition_requiring_routine_follow_up():
    """
    Test: Happy path - mild condition requiring routine follow-up
    
    Success criteria: Correctly assigns Tier 3 with reasoning that condition is stable and non-urgent
    """
    graph = create_graph()
    
    initial_state = {
        "messages": [
            HumanMessage(content="Patient is a 32-year-old female with occasional mild knee pain during exercise, no swelling, no redness. Pain started 3 months ago. X-ray shows no fractures or significant abnormalities. ROM is normal. Doctor suspects mild patellofemoral syndrome. Patient able to perform daily activities without limitation.")
        ]
    }
    
    result = await graph.ainvoke(initial_state)
    
    final_message = result["messages"][-1]
    response_text = final_message.content.lower()
    
    # Verify Tier 3 assignment
    assert "tier 3" in response_text or "triage tier 3" in response_text
    
    # Verify reasoning about non-urgent nature
    assert any(term in response_text for term in ["routine", "non-urgent", "stable", "benign"])


@pytest.mark.asyncio
async def test_happy_path_moderate_condition_requiring_timely_intervention():
    """
    Test: Happy path - moderate condition requiring timely intervention
    
    Success criteria: Correctly assigns Tier 2 recognizing need for timely intervention without emergency status
    """
    graph = create_graph()
    
    initial_state = {
        "messages": [
            HumanMessage(content="Patient is a 45-year-old male with newly diagnosed hypertension. BP readings consistently 155/98 over past 2 weeks. No end-organ damage detected. Fundoscopic exam normal, creatinine 1.1, no proteinuria. Patient asymptomatic but has family history of stroke. Doctor recommends starting antihypertensive therapy and lifestyle modifications.")
        ]
    }
    
    result = await graph.ainvoke(initial_state)
    
    final_message = result["messages"][-1]
    response_text = final_message.content.lower()
    
    # Verify Tier 2 assignment
    assert "tier 2" in response_text or "triage tier 2" in response_text
    
    # Verify reasoning about timely but non-emergent care
    assert any(term in response_text for term in ["timely", "within", "non-emergent", "not emergency"])


@pytest.mark.asyncio
async def test_happy_path_suspicious_mass_requiring_prompt_evaluation():
    """
    Test: Happy path - suspicious mass requiring prompt evaluation
    
    Success criteria: Correctly assigns Tier 2 for likely malignancy requiring prompt but not immediate intervention
    """
    graph = create_graph()
    
    initial_state = {
        "messages": [
            HumanMessage(content="Patient is a 62-year-old female with painless breast lump discovered 2 weeks ago. Lump is hard, irregular, fixed, approximately 2.5 cm. Mammogram shows suspicious mass with microcalcifications, BI-RADS 5. No distant symptoms. Patient otherwise healthy. Doctor recommends urgent biopsy and oncology referral.")
        ]
    }
    
    result = await graph.ainvoke(initial_state)
    
    final_message = result["messages"][-1]
    response_text = final_message.content.lower()
    
    # Verify Tier 2 assignment
    assert "tier 2" in response_text or "triage tier 2" in response_text
    
    # Verify reasoning about malignancy suspicion
    assert any(term in response_text for term in ["malignancy", "cancer", "bi-rads", "suspicious"])


@pytest.mark.asyncio
async def test_happy_path_acute_appendicitis_requiring_urgent_surgery():
    """
    Test: Happy path - acute appendicitis requiring urgent surgery
    
    Success criteria: Correctly assigns Tier 1 for acute appendicitis requiring urgent surgery
    """
    graph = create_graph()
    
    initial_state = {
        "messages": [
            HumanMessage(content="Patient is a 28-year-old male with 12 hours of progressive right lower quadrant pain, started periumbilical then localized. Fever 38.5°C, nausea, one episode of vomiting. Rebound tenderness and guarding present. WBC 16,000 with left shift. CT scan shows inflamed appendix with surrounding fat stranding, no perforation. Doctor diagnosis: acute appendicitis.")
        ]
    }
    
    result = await graph.ainvoke(initial_state)
    
    final_message = result["messages"][-1]
    response_text = final_message.content.lower()
    
    # Verify Tier 1 assignment
    assert "tier 1" in response_text or "triage tier 1" in response_text
    
    # Verify reasoning about surgical emergency
    assert any(term in response_text for term in ["appendicitis", "surgical", "immediate", "urgent surgery"])


@pytest.mark.asyncio
async def test_happy_path_seasonal_allergies_with_mild_symptoms():
    """
    Test: Happy path - seasonal allergies with mild symptoms
    
    Success criteria: Correctly assigns Tier 3 for mild allergic rhinitis requiring no urgent care
    """
    graph = create_graph()
    
    initial_state = {
        "messages": [
            HumanMessage(content="Patient is a 25-year-old female with sneezing, clear nasal discharge, itchy eyes for 1 week. Symptoms worse in morning, improves indoors. No fever, no shortness of breath. Lungs clear on auscultation. Patient has history of seasonal allergies. Vital signs normal. Doctor suspects allergic rhinitis.")
        ]
    }
    
    result = await graph.ainvoke(initial_state)
    
    final_message = result["messages"][-1]
    response_text = final_message.content.lower()
    
    # Verify Tier 3 assignment
    assert "tier 3" in response_text or "triage tier 3" in response_text
    
    # Verify reasoning about benign condition
    assert any(term in response_text for term in ["allergic", "rhinitis", "benign", "mild"])


@pytest.mark.asyncio
async def test_edge_case_ambiguous_symptoms_with_conflicting_indicators():
    """
    Test: Edge case - ambiguous symptoms with conflicting indicators
    
    Success criteria: Appropriately assigns Tier 2 when diagnosis is uncertain but cardiac etiology cannot be excluded
    """
    graph = create_graph()
    
    initial_state = {
        "messages": [
            HumanMessage(content="Patient is a 50-year-old male with intermittent chest discomfort for 3 days, described as 'pressure' but also 'sharp'. Pain sometimes related to movement, sometimes not. No radiation. Vital signs normal. ECG shows nonspecific ST-T changes. Troponin normal. Patient anxious, recently lost job. Doctor uncertain if cardiac or musculoskeletal vs anxiety.")
        ]
    }
    
    result = await graph.ainvoke(initial_state)
    
    final_message = result["messages"][-1]
    response_text = final_message.content.lower()
    
    # Verify Tier 2 assignment (cautious approach)
    assert "tier 2" in response_text or "triage tier 2" in response_text
    
    # Verify reasoning acknowledges uncertainty
    assert any(term in response_text for term in ["uncertain", "ambiguous", "further", "testing", "evaluation"])


@pytest.mark.asyncio
async def test_edge_case_borderline_vital_signs_in_elderly_patient():
    """
    Test: Edge case - borderline vital signs in elderly patient
    
    Success criteria: Correctly identifies high-risk situation in elderly patient with borderline findings requiring immediate evaluation
    """
    graph = create_graph()
    
    initial_state = {
        "messages": [
            HumanMessage(content="Patient is an 82-year-old female with generalized weakness for 2 days. BP 95/60 (baseline 110/70), HR 92, temp 37.8°C. Mild confusion, oriented to person and place but not time. Labs show sodium 132, creatinine 1.4 (baseline 1.0). No obvious infection source. Patient lives alone, has been eating and drinking poorly. Doctor notes possible dehydration vs early sepsis vs medication effect.")
        ]
    }
    
    result = await graph.ainvoke(initial_state)
    
    final_message = result["messages"][-1]
    response_text = final_message.content.lower()
    
    # Verify Tier 1 assignment (elderly with altered mental status)
    assert "tier 1" in response_text or "triage tier 1" in response_text
    
    # Verify reasoning about elderly risk
    assert any(term in response_text for term in ["elderly", "altered mental status", "confusion", "deteriorate"])


@pytest.mark.asyncio
async def test_error_scenario_missing_critical_test_results():
    """
    Test: Error scenario - missing critical test results
    
    Success criteria: Appropriately assigns highest tier based on clinical findings despite missing imaging, noting limitations
    """
    graph = create_graph()
    
    initial_state = {
        "messages": [
            HumanMessage(content="Patient is a 55-year-old male with severe abdominal pain for 6 hours, constant, 8/10 intensity. Pain in epigastric region. Patient vomiting. Vital signs: BP 100/65, HR 105, temp 37.2°C. Abdomen rigid with guarding. Doctor suspects perforated viscus but imaging not yet available.")
        ]
    }
    
    result = await graph.ainvoke(initial_state)
    
    final_message = result["messages"][-1]
    response_text = final_message.content.lower()
    
    # Verify Tier 1 assignment based on clinical findings
    assert "tier 1" in response_text or "triage tier 1" in response_text
    
    # Verify reasoning addresses surgical emergency
    assert any(term in response_text for term in ["surgical", "emergency", "acute abdomen", "peritoneal"])


@pytest.mark.asyncio
async def test_error_scenario_vague_symptoms_without_objective_findings():
    """
    Test: Error scenario - vague symptoms without objective findings
    
    Success criteria: Appropriately identifies data insufficiency while providing provisional guidance and requesting more information
    """
    graph = create_graph()
    
    initial_state = {
        "messages": [
            HumanMessage(content="Patient reports 'not feeling well' for several weeks. Denies specific symptoms when asked. Says everything hurts but can't localize. No fever documented. Patient states 'doctors never help me.' No physical exam findings documented. No test results provided. Doctor notes patient seems frustrated.")
        ]
    }
    
    result = await graph.ainvoke(initial_state)
    
    final_message = result["messages"][-1]
    response_text = final_message.content.lower()
    
    # Verify response acknowledges insufficient data
    assert any(term in response_text for term in ["insufficient", "limited", "additional", "more information", "incomplete"])
    
    # Should still provide some tier assignment (likely Tier 3 given no acute findings)
    assert "tier" in response_text